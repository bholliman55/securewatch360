# Airtable setup (SecureWatch360)

This document is a **placeholder** for v1. When the product needs Airtable (bases, tables, personal access tokens, webhooks, or sync jobs), add step-by-step instructions here:

1. Which Airtable **base** and **table(s)** the app uses.
2. How to create a **token** with least privilege and where to store it (e.g. environment variable name only; never commit the value).
3. Whether reads/writes run from **Inngest** (recommended for scheduled or long syncs) or from a **Route Handler** (short requests only).
4. How to **verify** the connection (e.g. one-off health job or a manual script).

Until Airtable is in scope, no Airtable packages are required in this repository.
