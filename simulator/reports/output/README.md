# Human simulation reports (generated)

Markdown and JSON written by `writeSimulationRunReports` default here. Filenames include the simulation `runId`.

Regenerate from persisted `.simulation-results/` with:

```bash
npm run sim:report -- --runId <uuid>
```

This directory is gitignored for ephemeral files; committed samples live under `docs/simulator/`.
